# 2026-05-18 작업 로그

## 작업 원칙

- 사용자의 중간 질문은 기존 예정 작업 변경으로 간주하지 않는다.
- 커밋 메시지는 `[Feat]`, `[Fix]`, `[Docs]` 같은 머릿말과 한국어 설명을 사용한다.
- UI 작업은 에뮬레이터 검증을 우선하되, 실제 데이터 주입 경로가 없는 화면은 테스트 검증을 먼저 완료한다.

## 완료한 작업

### 상세 화면 1차 UI polish

- 상세 화면 상단을 가맹점, 상태, 금액이 먼저 보이는 요약 카드로 재구성했다.
- `거래 정보` 카드를 추가해 결제 시간, 상태, 확인 방식, 동기화, 카테고리를 정리했다.
- 피드백 영역을 `이 거래가 실제 지출인가요?` 카드로 분리해 사용자가 해야 할 일을 명확히 했다.
- `판별 근거` 카드를 홈/내역과 같은 rounded white card 스타일로 정리했다.
- 파싱 후보, 분류 chip, 신뢰도, reason code 표시를 더 읽기 쉬운 간격과 색상으로 조정했다.
- 제외된 거래는 상단 금액에 취소선을 적용해 총액에서 제외된 거래임을 시각적으로 드러내도록 했다.

### 개발자용 샘플 데이터 주입

- 설정 화면에 debug mode 전용 `개발자 도구` 카드를 추가했다.
- `샘플 거래 생성` 버튼으로 에뮬레이터 UI 확인용 거래 3건을 생성할 수 있게 했다.
- 샘플 데이터는 raw notification, transaction candidate, classification result, expense transaction을 함께 저장한다.
- 샘플 거래는 `배달의민족`, `동백전 충전`, `스타벅스`로 구성해 확정 지출과 확인 필요 거래를 동시에 확인할 수 있게 했다.
- 반복 실행 시 같은 ID를 덮어쓰도록 구성해 샘플 거래가 계속 중복 누적되지 않게 했다.

## 검증 기록

- `flutter test test\features\transaction_detail\transaction_detail_screen_test.dart`: 3개 통과
- `dart format lib test`: 완료
- `flutter analyze`: 이슈 없음
- `flutter test`: 50개 모두 통과
- `flutter build apk --debug`: 성공
- `flutter test test\features\settings\settings_screen_test.dart`: 4개 통과
- `flutter analyze`: 이슈 없음
- `flutter test`: 51개 모두 통과
- `flutter build apk --debug`: 성공
- `flutter devices`: 현재 Android 에뮬레이터 미연결
- `adb devices`: 현재 Android 에뮬레이터 미연결
- `flutter devices`: `emulator-5554` 인식 확인
- `flutter run -d emulator-5554`: debug 앱 실행 확인
- 설정 화면 `샘플 거래 생성`: 샘플 거래 주입 확인
- `adb screencap`: 설정/홈/내역/상세 화면 캡처 완료

## 현재 평가

- 홈/내역/상세의 주요 조회 흐름이 같은 브랜드 톤으로 맞춰졌다.
- 상세 화면은 이제 단순 데이터 나열보다 “이 거래가 무엇이고, 왜 이렇게 판별됐고, 사용자가 어떻게 수정할 수 있는지”가 더 분명하다.
- 샘플 거래 주입 기능이 생겨 에뮬레이터에서 홈/내역/상세 화면을 실제 데이터 상태로 검증할 준비가 됐다.
- 샘플 거래가 들어간 홈/내역/상세 화면 스크린샷을 확보해 이후 UI 회귀 비교 기준으로 사용할 수 있다.
- 설정 화면은 아직 기본 카드 UI에 가까워 다음 polish 대상이다.

## 다음 후보 작업

- 설정/진단 화면 1차 UI polish를 진행한다.
- 샘플 데이터가 있는 상태에서 상세 화면 판별 근거 하단까지 스크롤 캡처를 추가한다.
- 실제 Android 기기가 준비되면 권한과 백그라운드 수집을 검증한다.

## 스크린샷 산출물

- `docs/screenshots/billlearn-settings-before-seed.png`
- `docs/screenshots/billlearn-home-seeded.png`
- `docs/screenshots/billlearn-history-seeded.png`
- `docs/screenshots/billlearn-detail-seeded.png`
