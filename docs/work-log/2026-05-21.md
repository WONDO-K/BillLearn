# 2026-05-21 작업 로그

## 작업 원칙

- 전날 남긴 다음 후보 작업을 기준으로 진행한다.
- 질문 답변은 기존 예정 작업을 바꾸지 않는 범위에서 처리한다.
- 커밋 메시지는 `[Feat]`, `[Fix]`, `[Docs]` 머릿말과 한국어 설명을 사용한다.

## 완료한 작업

### Android raw event pending queue 추가

- Android native에서 알림/SMS raw event를 `SharedPreferences` pending queue에 먼저 저장하도록 추가했다.
- Flutter가 시작될 때 `drainPendingRawEvents` method channel로 pending event를 회수해 기존 raw notification pipeline에 넣도록 연결했다.
- live EventChannel 이벤트와 pending drain 이벤트가 중복될 수 있으므로 기존 `sourceHash` 중복 방어를 유지했다.
- `NotificationListenerService`, `SmsReceiver` 모두 새 queue 저장 경로를 사용하도록 변경했다.

### Pending queue debug 진단 표시

- Android bridge에 `getPendingRawEventCount` method channel 계약을 추가했다.
- native pending queue의 현재 잔여 건수를 설정 화면 개발자 도구 카드에 표시하도록 했다.
- 실제 기기 검증 시 `대기 중 원천 이벤트 N건`과 `수집 진단` 저장 건수를 분리해서 볼 수 있게 했다.

## 검증 기록

- 실패 확인: `flutter test test\platform\android_event_bridge_test.dart test\app\raw_notification_pipeline_test.dart`
  - `drainPendingRawEvents`, `pendingRawNotificationsProvider` 미구현으로 실패하는 것을 먼저 확인했다.
- `dart format lib test`: 완료
- `flutter test test\platform\android_event_bridge_test.dart test\app\raw_notification_pipeline_test.dart`: 5개 통과
- 실패 확인: `flutter test test\platform\android_event_bridge_test.dart test\features\settings\settings_screen_test.dart`
  - `getPendingRawEventCount` 미구현과 설정 화면 미표시로 실패하는 것을 먼저 확인했다.
- `flutter test test\platform\android_event_bridge_test.dart test\features\settings\settings_screen_test.dart`: 8개 통과
- `flutter analyze`: 이슈 없음
- `flutter build apk --debug`: 성공

## 현재 평가

- 앱이 늦게 켜져도 native receiver/service가 실행됐다면 raw event를 다음 시작 시 회수할 수 있는 최소 안전장치가 생겼다.
- 설정 화면에서 pending queue 잔여 건수를 볼 수 있어 실제 기기 검증 때 누락 지점을 더 빨리 분리할 수 있다.
- 실제 Android 기기에서 강제 종료 후 receiver/service가 실행되는지는 제조사/OS 정책 영향을 받으므로 아직 미검증이다.

## 다음 후보 작업

- 전체 `flutter test`를 실행하고 에뮬레이터 설치까지 확인한다.
- 홈/내역 거래 행의 실제 브랜드 로고 asset 적용 방식을 정리한다.
